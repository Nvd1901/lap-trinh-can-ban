#include <stdio.h>

int main()
{
    printf("moi ban nhap hai so ");
    int a,b;
    int x=1;
    scanf(" %d %d", &a,&b);
    for (int i=1;i<=a && i<=b;i++){
        if (a%i==0 && b%i==0){
             x=i;
        }
    }
    int y= (a*b)/x;
    printf("uoc chung lon nhat la %d va boi chung nho nhat la %d",x,y);
    return 0;
}
