#include <stdio.h>

int main()
{
    int x;
    int y=0;
    printf("moi nhap so");
    scanf(" %d", &x);
    for(int i=0;i<9;i++){
         y=y+x;
        scanf(" %d",&x);
     }
    printf("tong la %d",y);

    return 0;
}
