#include <stdio.h>

int main()
{
    float a,b,c,d,e,f;
    scanf ( "%f %f %f %f %f %f", &a,&b,&c,&d,&e,&f);
    float x=(e*c-b*f)/(a*e-d*b);
    float y=(a*f-d*c)/(a*e-d*b);
    printf("he nghiem la x=%f va y=%f",x,y);

    return 0;
}
