#include <stdio.h>

int main()
{
    printf("moi ban nhap so ");
    int a;
    int x=0;
    scanf("%d", &a);
    for (int i=1;i<=a;i++ ){
        if (a%i==0 ){
             x=x+i;
        }
    }
    if(a*2==x){
        printf("day la so hoan hao");
    }
    else{
        printf("day khong phai so hoan hao");
    }
    return 0;
}
