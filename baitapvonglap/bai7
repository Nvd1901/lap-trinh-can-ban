#include <stdio.h>

int main()
{
    printf("moi ban nhap so ");
    int n;
    float x=0;
    scanf("%d", &n);
    for (int i=1;i<=n;i++ ){
        x=x+(1.0/i);      
    }
    printf("S=%f",x);
    return 0;
}
